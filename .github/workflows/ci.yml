name: CI (stub)

on: [push, pull_request]

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder step
        run: echo "Add CI steps (lint, tests) here"
