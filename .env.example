# Copy to .env and fill
DJANGO_SECRET_KEY=replace_me
DJANGO_DEBUG=True
POSTGRES_DB=dashboard_db
POSTGRES_USER=dashboard_user
POSTGRES_PASSWORD=changeme
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
JWT_SECRET_KEY=replace_me
SIMPLE_JWT_SIGNING_KEY="very-long-random-secret-keep-private"
DJANGO_SECRET_KEY="(your django secret, optional)"
FASTAPI_CORS_ORIGINS=http://localhost:5173
DJANGO_CORS_ORIGINS=http://localhost:5173
FASTAPI_BATCH_URL=http://localhost:8001/v1/batch/dc_project  # FastAPI batch endpoint (we create)
FASTAPI_BASE_URL=http://localhost:8001/v1

# backend-django/.env (or repo .env used by both)
SIMPLE_JWT_SIGNING_KEY=very-long-random-secret-string-keep-private
JWT_ALGO=HS256

# ensure FastAPI has the same secret
FASTAPI_JWT_SECRET=very-long-random-secret-string-keep-private
FASTAPI_JWT_ALG=HS256

# base URLs (frontend Vite env)
VITE_FASTAPI_BASE=http://localhost:8001
VITE_DJANGO_BASE=http://localhost:8000/api/v1
